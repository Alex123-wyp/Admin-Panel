import React from "react";
import { Outlet } from "react-router-dom";

const Other = () => {
    return (
        <div>
            Other
            <Outlet/>
        </div>
    )
}

export default Other
